---
name: AI 작성 코드 학습용 — push 전 교육 주석 리뷰 워크플로우
description: AI가 작성한 비자명한 코드를 본인 자산으로 흡수하기 위해 push 전 "교육용 밀도 높은 주석" 을 먼저 요청해 리뷰한 뒤 승인하는 운영 방식
type: feedback
originSessionId: b24e4785-0839-4f63-bb54-a57015bd6df4
---
AI가 작성/리팩토링한 비자명한 코드는 push 전에 **교육용 밀도 높은 주석**을 먼저 요청해서 리뷰한 뒤 승인한다. 주석의 밀도는 "왜/동작원리/비유"까지 포함 가능하며, 이해가 굳어진 뒤엔 본 코드 주석을 슬림하게 줄일지 그대로 둘지 본인이 결정한다.

**Why:**
- AI 작성 코드는 인지부채 위험 HIGH (project_ai_written_code_cognitive_debt 연계)
- 학습 단계를 "주석 리뷰" 로 강제 삽입하면 `코드를 내 것으로 만드는` 장치가 됨
- 2026-04-21 resolveImageUrl T3 리팩토링 때 실제로 이 방식을 사용해 "머리속 이미지" 가 잡힐 때까지 주석을 읽고 승인 → 본인 스타일로 확립

**How to apply:**
- 리팩토링/새 로직/비자명한 유틸을 AI가 작성하면 push 전 "이해 체크 필요?" 짧게 확인
- 사용자가 학습 의도 밝히면: (1) 코드 변경은 이미 커밋 (2) 그 위에 교육용 주석을 별도 커밋으로 얹고 (3) 사용자 리뷰/질문 → 승인 → push
- 주석엔 함수 계약, 분기 규칙, 비유, 엣지 케이스 표, IIFE 같은 패턴의 "왜" 포함
- 리뷰 중 사용자가 "이미지 안 떠오름" 신호 주면 `user_comprehension_criterion` 적용해 비유/ASCII 구조로 다시 설명
- 승인 후엔 주석을 슬림화할지 그대로 둘지 사용자에게 확인 (기본은 그대로 유지, 짐이 되면 정리)

**주석을 슬림화/삭제할 때 기준 (2026-05-22 F-06 작업에서 확립):**
- **WHAT 주석 = 삭제 OK**: 코드가 뭘 하는지 그대로 풀어쓴 것 (코드 읽으면 바로 보임 = 중복).
- **WHY 주석 = 보존**: 설계 의도·가드레일·비자명한 방어 이유. 코드에는 안 적힌 정보라 지우면 영영 사라짐.
- 사례: F-06 때 사용자가 PostDetailPage 주석 4블록을 "기능 설명이라 길다"며 삭제했으나, 실제론 대부분 WHY(편집 state를 page-level에 둔 이유=모바일 포커스, PATCH fallback 방어 이유, flat 2레벨 답글 정책)였음 → WHY만 복원, F-06 커밋엔 404 변경만 남김.
- **안티패턴 박제 — "주석 지우고 궁금하면 AI한테 물어보면 됨"은 약한 논리:**
  1. 주석은 협업자·미래의 본인·다른 AI 세션 등 다른 독자도 봄 (메모리는 이 세션의 본인만 접근)
  2. 메모리에 다 있지도 않음 (회수 불가 위험)
  3. 회수 비용 비대칭 (코드 옆 한 줄 읽기 0초 vs 세션 열고 메모리 로드 바라기)
  4. 이 습관 자체가 사용자 핵심 목표인 'AI 의존 줄이기'와 정반대 — [[user_self_coding_goal]] / [[user_ai_dependency_anxiety]] 연계
