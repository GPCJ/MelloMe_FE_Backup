# 환경변수 (민감 정보)
.env
.env.*
.env.local
.env.production

# IDE
.idea/
.vscode/

# OS
.DS_Store

# 백엔드/인프라 파일 (프론트 레포에 불필요)
docker-compose.yml
openapi-3.0.yaml
/package-lock.json

# Claude (settings.local.json은 개인 설정이라 제외, worktrees는 로컬 전용)
.claude/settings.local.json
.claude/worktrees/
*/.claude/settings.local.json

# OMC (런타임 임시 상태 파일)
.omc/

# git object 복구 시 생성된 .git 백업 폴더 (2026-04-17 사고 재발 방지)
.git.backup_*/

# 토큰/인증 파일
*token*
*Token*
github\ token
